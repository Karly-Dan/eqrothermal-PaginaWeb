{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start": "node .output/server/index.mjs",
    "postinstall": "node -e \"try{require.resolve('nuxt/package.json');process.exit(0)}catch(e){process.exit(1)}\" && npx nuxt prepare || true"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^4.2.0"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "firebase": "^12.5.0",
    "firebase-admin": "^13.5.0",
    "nodemailer": "^7.0.10"
  }
}
